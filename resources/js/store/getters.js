// comments
export const comments = (state) => state.comments;

// articles
export const articles = (state) => state.articles;

// pagination details
export const pagination_details = (state) => state.pagination_details;

// categories
export const categories = (state) => state.categories;
